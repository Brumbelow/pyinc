[build-system]
requires = ["hatchling==1.31.0"]
build-backend = "hatchling.build"

[project]
name = "pyinc"
version = "3.1.2"
description = "A correctness-first incremental query engine for Python."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [
  { name = "Andrew Brumbelow", email = "andrew.brumbelow@gmail.com" }
]
keywords = ["incremental-computation", "query-kernel", "salsa", "red-green", "backdating", "provenance"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: Apache Software License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Software Development :: Libraries :: Python Modules",
  "Typing :: Typed",
]
dependencies = []

[project.scripts]
pyinc-tools = "pyinc_tools.cli:main"

[project.urls]
Homepage = "https://github.com/Brumbelow/pyinc"
Documentation = "https://github.com/Brumbelow/pyinc/blob/v3.1.2/docs/README.md"
Repository = "https://github.com/Brumbelow/pyinc"
Issues = "https://github.com/Brumbelow/pyinc/issues"
Changelog = "https://github.com/Brumbelow/pyinc/blob/v3.1.2/CHANGELOG.md"

[project.optional-dependencies]
dev = [
  "coverage==7.15.2",
  "hypothesis==6.163.0",
  "joblib==1.5.3",
  "mypy==2.3.0",
  "packaging==26.2",
  "pytest==9.1.1",
  "pytest-cov==7.1.0",
  "pytest-xdist==3.8.0",
  "ruff==0.16.0",
]
# Benchmark-only comparison dependencies. Never imported by src/pyinc or
# src/pyinc_codegen — used only by the bench/ harness.
bench = [
  "joblib==1.5.3",
]
release = [
  "build==1.5.0",
  "hatchling==1.31.0",
  "twine==6.2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pyinc", "src/pyinc_codegen", "src/pyinc_tools"]

[tool.hatch.build.targets.sdist]
include = [
  "/src",
  "/tests",
  "/examples",
  "/docs",
  "/bench",
  "/release",
  "/requirements",
  "/scripts",
  "/README.md",
  "/LICENSE",
  "/CHANGELOG.md",
  "/pyproject.toml",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q --tb=no"

[tool.coverage.run]
branch = true
source = ["pyinc", "pyinc_codegen", "pyinc_tools"]

[tool.coverage.report]
fail_under = 90
show_missing = true

[tool.mypy]
python_version = "3.11"
mypy_path = ["examples"]
warn_return_any = true
strict = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_reexport = true
warn_unused_ignores = true
warn_redundant_casts = true
warn_unused_configs = true
files = ["src", "tests", "bench", "scripts"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "TID"]
ignore = ["E501"]  # line length handled by formatter
